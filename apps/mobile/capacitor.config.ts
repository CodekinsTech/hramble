import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
	appId: "com.hramble.mobile",
	appName: "Hramble",
	webDir: "dist",
	backgroundColor: "#ffffff",
}

export default config
